# =============================================================================
# CGM Benchmark Evaluation — Requirements
# =============================================================================
#
# 安装:
#   pip install -r requirements.txt                         # 基础评测（Mock 模式）
#   pip install -r requirements.txt -r requirements-gpu.txt # 完整评测（含模型推理）
# =============================================================================

# ---------------------------------------------------------------------------
# 基础依赖 — evaluate.py --mock / --predictions 模式
# (纯 Python 标准库实现，以下仅为 Python 版本约束)
# ---------------------------------------------------------------------------
# Python >= 3.10（dataclasses, typing, difflib 等标准库已满足）

# ---------------------------------------------------------------------------
# 可选: 加速编辑距离计算（非必须，metrics.py 有纯 Python 回退）
# ---------------------------------------------------------------------------
# python-Levenshtein>=0.25.0    # C 扩展，可替代纯 Python Levenshtein（如需高性能）

# ---------------------------------------------------------------------------
# 完整模型推理 — evaluate.py --model-path 模式
# 需要额外安装以下依赖，或使用 requirements-gpu.txt
# ---------------------------------------------------------------------------

# --- 深度学习框架 ---
# torch>=2.0.0
# transformers>=4.45.0
# accelerate>=0.20.0

# --- Qwen3-VL 模型依赖 ---
# qwen-vl-utils>=0.0.6

# --- VT 模块依赖 ---
# Pillow>=9.0.0
# numpy>=1.24.0
# graphviz>=0.20.0        # Python 包装器；还需系统安装 graphviz 二进制: apt install graphviz
# networkx>=3.0

# --- 训练/推理加速（可选） ---
# bitsandbytes>=0.41.0     # 4-bit / 8-bit 量化
# peft>=0.7.0              # LoRA 适配器
# flash-attn>=2.5.0        # Flash Attention 2 加速
